# src/application

Use Case / orchestration / transaction境界。B-01 / DEVELOPMENT_STANDARDS §1。

`src/app` からの入力を受け、`src/core` のdomain logicと `src/adapters` を組み合わせる層。

Phase 0時点ではuse case未実装（CLAUDE.md Phase 0 scope）。
